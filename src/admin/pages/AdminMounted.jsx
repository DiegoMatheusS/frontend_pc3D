import { useEffect, useMemo, useState } from 'react'
import { Link } from 'react-router-dom'
import { adminService } from '../services/adminService'
import { AdminError, AdminLoading, AdminPageHeader, AdminStatus, EmptyRow, formatDate } from '../components/AdminCommon'
import { useAdminToast } from '../components/AdminToast'
export default function AdminMounted(){const toast=useAdminToast();const[items,setItems]=useState(null);const[error,setError]=useState(null);const[search,setSearch]=useState('');async function load(){try{setItems(await adminService.builds.list());setError(null)}catch(err){setError(err)}}useEffect(()=>{let active=true;adminService.builds.list().then(result=>{if(active){setItems(result);setError(null)}}).catch(err=>{if(active)setError(err)});return()=>{active=false}},[]);const filtered=useMemo(()=>(items||[]).filter(i=>!search||[i.nome,i.marca,i.modelo,i.categoria,i.finalidade].join(' ').toLowerCase().includes(search.toLowerCase())),[items,search]);async function toggle(item){try{await adminService.builds.update(item.id,{publicado:!item.publicado});toast.show(item.publicado?'PC despublicado.':'PC publicado.');await load()}catch(err){toast.show(err.message,'erro')}}async function remove(item){if(!window.confirm(`Arquivar “${item.nome}”?`))return;try{await adminService.builds.remove(item.id);toast.show('PC montado arquivado.');await load()}catch(err){toast.show(err.message,'erro')}}if(error)return <AdminError error={error}/>;if(!items)return <AdminLoading/>;return <><AdminPageHeader title="PCs Montados" description="Builds comerciais/recomendadas exibidas na área Montados."><Link className="btn btn-primario" to="/admin/montados/novo">+ Novo PC montado</Link></AdminPageHeader><section className="admin-toolbar admin-toolbar--2"><label className="admin-toolbar-field"><span>Pesquisar</span><input className="admin-input" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Nome, categoria ou finalidade"/></label></section><section className="admin-table-card"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>PC</th><th>Categoria</th><th>Componentes</th><th>Status</th><th>Atualização</th><th>Ações</th></tr></thead><tbody>{filtered.length?filtered.map(item=><tr key={item.id}><td><strong>{item.nome}</strong><br/><small>{item.marca||''} {item.modelo||''}</small></td><td>{item.categoria||item.finalidade||'—'}</td><td>{item.componentes?.length??'—'}</td><td><AdminStatus published={item.publicado} active={item.ativo}/></td><td>{formatDate(item.atualizadoEm)}</td><td><div className="admin-row-actions"><Link className="admin-action-button" to={`/admin/montados/${item.id}`}>Editar</Link><button className="admin-action-button" onClick={()=>toggle(item)} type="button">{item.publicado?'Despublicar':'Publicar'}</button><button className="admin-action-button" onClick={()=>remove(item)} type="button">Arquivar</button></div></td></tr>):<EmptyRow columns={6}/>}</tbody></table></div></section></>}
