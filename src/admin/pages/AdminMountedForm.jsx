import { useEffect, useState } from 'react'
import { useNavigate, useParams } from 'react-router-dom'
import { adminService } from '../services/adminService'
import { AdminBack, AdminError, AdminLoading, AdminPageHeader } from '../components/AdminCommon'
import { useAdminToast } from '../components/AdminToast'
const EMPTY={nome:'',marca:'',modelo:'',descricao:'',imagemUrl:'',categoria:'',finalidade:'',resolucaoRecomendada:'',publicado:false,ativo:true,componentes:'[]',configuracao3D:'{}'}
export default function AdminMountedForm(){const{id}=useParams();const editing=id&&id!=='novo';const navigate=useNavigate();const toast=useAdminToast();const[form,setForm]=useState(EMPTY);const[loading,setLoading]=useState(Boolean(editing));const[error,setError]=useState(null);const[saving,setSaving]=useState(false);useEffect(()=>{if(!editing)return;let active=true;adminService.builds.get(id).then(item=>active&&setForm({...EMPTY,...item,componentes:JSON.stringify(item.componentes||[],null,2),configuracao3D:JSON.stringify(item.configuracao3D||{},null,2)})).catch(err=>active&&setError(err)).finally(()=>active&&setLoading(false));return()=>{active=false}},[editing,id]);const update=(k,v)=>setForm(c=>({...c,[k]:v}));async function submit(e){e.preventDefault();setSaving(true);setError(null);try{let componentes,configuracao3D;try{componentes=JSON.parse(form.componentes||'[]');configuracao3D=JSON.parse(form.configuracao3D||'{}')}catch{throw new Error('Revise os campos JSON da build.')}const body={nome:form.nome.trim(),marca:form.marca.trim()||undefined,modelo:form.modelo.trim()||undefined,descricao:form.descricao.trim()||undefined,imagemUrl:form.imagemUrl.trim()||undefined,categoria:form.categoria.trim()||undefined,finalidade:form.finalidade.trim()||undefined,resolucaoRecomendada:form.resolucaoRecomendada.trim()||undefined,publicado:Boolean(form.publicado),ativo:Boolean(form.ativo),componentes,configuracao3D};const saved=editing?await adminService.builds.update(id,body):await adminService.builds.create(body);toast.show('PC montado salvo.');navigate(`/admin/montados/${saved?.id||id}`,{replace:true})}catch(err){setError(err)}finally{setSaving(false)}}if(loading)return <AdminLoading/>;if(error&&editing&&!form.nome)return <AdminError error={error}/>;return <><AdminPageHeader title={editing?'Editar PC montado':'Novo PC montado'} description="Os componentes usam hardwareId conforme o contrato atual de PCs Montados do backend."><AdminBack to="/admin/montados">Cancelar</AdminBack></AdminPageHeader><form className="admin-form-layout" onSubmit={submit}><div className="admin-form-card"><section className="admin-form-section"><h2>Identificação</h2><div className="admin-form-grid"><div className="admin-field full"><label>Nome</label><input className="admin-input" required value={form.nome} onChange={e=>update('nome',e.target.value)}/></div><div className="admin-field"><label>Marca</label><input className="admin-input" value={form.marca} onChange={e=>update('marca',e.target.value)}/></div><div className="admin-field"><label>Modelo</label><input className="admin-input" value={form.modelo} onChange={e=>update('modelo',e.target.value)}/></div><div className="admin-field"><label>Categoria</label><input className="admin-input" value={form.categoria} onChange={e=>update('categoria',e.target.value)}/></div><div className="admin-field"><label>Finalidade</label><input className="admin-input" value={form.finalidade} onChange={e=>update('finalidade',e.target.value)}/></div><div className="admin-field full"><label>Descrição</label><textarea className="admin-textarea" value={form.descricao} onChange={e=>update('descricao',e.target.value)}/></div></div></section><section className="admin-form-section"><h2>Componentes</h2><div className="admin-field"><label>Componentes JSON</label><textarea className="admin-textarea admin-code-area" value={form.componentes} onChange={e=>update('componentes',e.target.value)}/><small className="admin-help">Formato: [{`{ "hardwareId": 1, "categoria": "PROCESSADOR", "quantidade": 1 }`}].</small></div></section><section className="admin-form-section"><h2>Configuração 3D</h2><textarea className="admin-textarea admin-code-area" value={form.configuracao3D} onChange={e=>update('configuracao3D',e.target.value)}/></section>{error&&<section className="admin-form-section"><p className="admin-form-error">{error.message}</p></section>}<footer className="admin-form-footer"><button className="btn btn-primario" type="submit" disabled={saving}>{saving?'Salvando...':'Salvar PC montado'}</button></footer></div><aside className="admin-sticky-side"><div className="admin-card"><div className="admin-card-body"><label className="admin-switch"><input type="checkbox" checked={form.publicado} onChange={e=>update('publicado',e.target.checked)}/> Publicado</label><br/><br/><label className="admin-switch"><input type="checkbox" checked={form.ativo} onChange={e=>update('ativo',e.target.checked)}/> Ativo</label></div></div></aside></form></>}
